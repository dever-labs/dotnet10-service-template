apiVersion: skaffold/v4beta11
kind: Config
metadata:
  name: service-template

# The app runs locally via `make dev` (mirrord + dotnet watch).
# Use `make fake` to start the mockly fake server alongside it.

build:
  artifacts: []

deploy:
  helm:
    releases: []
